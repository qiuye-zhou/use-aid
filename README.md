# use-aid
Utility set for Vue3 only

# Get started
```
pnpm add use-aid
yarn add use-aid
npm install use-aid
```
```
import { ref } from 'vue'
import { useMedia } from 'use-aid'

const isMedia = ref(useMedia())
```