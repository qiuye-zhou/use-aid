# useaid
Utility set for Vue3 only

# Get started
```
pnpm add useaid
yarn add useaid
npm install useaid
```
```
import { ref } from 'vue'
import { useMedia } from 'useaid'

const isMedia = ref(useMedia())
```